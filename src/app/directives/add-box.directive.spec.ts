import { AddBoxDirective } from './add-box.directive';

describe('AddBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBoxDirective();
    expect(directive).toBeTruthy();
  });
});
